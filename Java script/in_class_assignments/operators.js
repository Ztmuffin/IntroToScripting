/* 
< less than
<= less than or equal to
*/ 
if (10<=20) {
    console.log("10 is less than 20");
}
 // > greater than
 // >= greater than or equal to

 // = assign to variable
 var myPassword = "Bananas";
// == compare values
    if (2==2) {
        console.log("one is equal to one");
    }
// != not equal to
    if (6!=1) {
        console.log ("5 is not equal to 1");
    }
// + add
var plums = 10+5;
// += add to current values
plums += plums
// - subtract
plums - 10;
// -= subtract from
plums -= plums
// * multiply
var peaches =10*10;
// / divide
peaches / 5;
//  ++ increment (add one to)
peaches ++;
// -- degrement (subtract one from)
peaches --;

// &&  and
if (plums >= 0 && peaches >2) {
    console.log (plums);
    console.log (peaches);
}
// || or
if (plums < 0 || peaches >=2) {
    console.log ("one or the other is true");
}