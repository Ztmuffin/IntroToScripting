var maxSpeed = 110;
var currentSpeed =75;

var statement = "You're going to slow, your speed is " + currentSpeed +
                             "mph , you can go up to " + maxSpeed + "mph.";
                                    + "which is "+ (maxSpeed-currentSpeed) + "Mph faster."

console.log(statement);
alert(statement);

// look at this in chrome 